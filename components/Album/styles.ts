import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        margin: 20,
        marginRight: 0
    },
    text: {
        color: 'grey',
        marginTop: 10,
    }
})

export default styles;